repos:
  - repo: local
    hooks:
      - id: pedantic
        name: pedantic
        entry: make pedantic
        language: system
        pass_filenames: false
        always_run: true
        stages: [pre-commit]